# Test-only dependencies. Not installed on the deployed service — see
# requirements.txt for that. Install into the same venv as requirements.txt:
#   ./venv/bin/pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
